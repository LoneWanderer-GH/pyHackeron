with "../corelec_ada.gpr";

project Decode_Smoke is
   for Main use ("decode_smoke.adb");
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat2022", "-O2");
   end Compiler;
end Decode_Smoke;
